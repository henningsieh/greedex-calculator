[phases.setup]
nixPkgs = ['bun', 'libgcc', 'gcc.cc.lib']

[phases.install]
cmds = ['bun install --frozen-lockfile']

[phases.build]
cmds = ['bun run build']

[start]
cmd = 'bun run prestart && bun run start'
