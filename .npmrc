# Bun Configuration
# Bun uses its own configuration system via bunfig.toml
# This file is kept for compatibility with npm/other tools

# Link workspace packages
link-workspace-packages=true

# Lockfile - strict validation
lockfile=true

# Module resolution speed optimization
shamefully-hoist=false

# Development dependency deduplication
dedupe-peer-dependents=true
